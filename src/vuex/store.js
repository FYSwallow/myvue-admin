import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)
export default new Vuex.Store({
    state: {
        tagList: []
    },
    mulations: {
        addRouter(state, obj) {
            // var list = state.tagList
            // for(var i = 0; i< list.length; i++) {
            // var list = state.tagList
            //     if(list[i].name && list[i].name !== obj.name) {
            //         list.
            //     }
            // }
        }
    },
    getters: {

    }
})